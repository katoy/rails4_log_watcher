<h1>Log:<%= @log %> (<%= @env %>)</h1>

<div id="file-list center">
  <%= check_box_tag 'autoRefresh', true, false, {:disabled => true, :title => '未実装...'} %>自動更新
  <div><button class="wg-external-csv-export-button" data-grid-name="grid">CSV エクスポート (utf-8)</button></div>
  <br>
  表示<%= select_tag('perPage',
  	  options_for_select([
  	      ['2', '4'],
	    ['5', '5'],
	    ['10', '10'],
	    ['20', '20'],
	    ['50', '50'],
	    ['100', '100'],
	    ['200', '200'],
	    ['400', '400'],
	    ['1000', '1000']
	  ],
	  @perPage.to_i),
	  {:id => 'perPage'})
	  %>件
  <%= render 'grid_grid' %>
</div>
